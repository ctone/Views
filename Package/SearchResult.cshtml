@model List<DiethelmTSWebsite2015.Models.PackageSearchResult>

@{
    Layout = null;
}

@using (Html.BeginForm("Detail", "Package", FormMethod.Post, new { id = "frmDetail"}))
{
        <div class="small-12 columns search-result-area">
            <div class="row">
                @if((Model != null) && (Model.Count() > 0))
                {
                    foreach (var item in Model)
                    {
                        @Html.DisplayFor(modelItem => item, "PackageSearchItem")
                    }
                }
                else
                {
                //TODO:
                }
            </div>
        </div>
    <script>
        function GotoDetail(packageID, packageCode) {

            $("#PackageID").val(packageID);
            $("#frmDetail").submit();

            @*$.ajax({
url: '@Url.Action("Detail", "Package")',
type: 'POST',
data: {PACKAGE_ID: packageID , PACKAGE_CODE: packageCode},
success: function (data) {

}
});*@
        }
    </script>
    }
